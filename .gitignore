# 임시 파일
temp/
uploads/
*.log
*.tmp

# Node modules (이미 있을 수 있음)
node_modules/

# Docker 관련
.env
.env.local

# 큰 바이너리 파일
*.mp3
*.mp4
*.wav
*.aac
*.txt

.DS_Store

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Railway 관련 파일 (더 이상 사용하지 않음)
railway.json

# Docker 관련 파일들은 이제 Git에 포함됨
# Dockerfile - Git에 포함
# docker-compose.yml - Git에 포함
# .dockerignore - Git에 포함

# Windows 배치 스크립트는 Git에 포함함 (Windows PC에서 필요)
